version: "3.8"
services:
  genetropica:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
    environment:
      # Demo mode: uses synthetic mock data. Set to "false" for real pipeline data.
      - USE_MOCK_DATA=true
