import { Section } from '../components/Section'

export default function Explore() {
  return (
    <Section eyebrow="Tool 01" title="Drug Explorer">
      Coming in a later phase: filter and sort all 100 candidates per target, with the dual-metric ranking.
    </Section>
  )
}
