import { Section } from '../components/Section'

export default function Methods() {
  return (
    <Section eyebrow="Tool 07" title="Methods">
      Coming in a later phase: exactly how it was run, the docking grid, the ML prior, and the data sources.
    </Section>
  )
}
