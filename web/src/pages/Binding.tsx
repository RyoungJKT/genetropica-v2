import { Section } from '../components/Section'

export default function Binding() {
  return (
    <Section eyebrow="Tool 02" title="3D Binding Viewer">
      Coming in a later phase: a docked pose inside its protein pocket, with the predicted contact residues.
    </Section>
  )
}
